# The working corpus and crash artifacts are generated, not authored. seeds/ is
# not here on purpose: it is the minimized corpus, committed, and replayed on
# stable by dhow-codec's replay_test, so the inputs that reach the interesting
# code are checked by everyone on every run rather than only where cargo-fuzz
# is installed.
corpus/
artifacts/
coverage/
target/
