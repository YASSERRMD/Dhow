# Corpora and crash artifacts are generated, not authored. regressions/ is
# not here on purpose: it is curated, committed, and replayed on stable by
# dhow-codec's replay_test, so an input that once broke a parser is checked by
# everyone on every run rather than only where cargo-fuzz is installed.
corpus/
artifacts/
coverage/
target/
